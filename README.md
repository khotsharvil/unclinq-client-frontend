# Unclinq — Client App (front-end)

React + Vite + Tailwind client PWA. Screens in `src/pages/client/`; design system in `src/index.css` (scoped `[data-theme=client]`).

```
npm install
npm run dev   # http://localhost:5173
```
Calls the backend on `/api` (proxy in vite.config.js).
